<div class="container">
  <div class="row">
    <div class="col">
      <h4 class="text-center">Football daysへようこそ！！</h4>
      <p class="text-center">ログインをすると記事の投稿・コメントができます！</p>
      <div class="card col">
        <p class="text-center">最近の投稿</p>
        <% @articles.each do |article| %>
        <div class="card-header">
          <table class="table" style="table-layout: fixed;"> 
            <tr>
              <td style="word-wrap: break-word;">
              <%= image_tag article.user.get_image(50, 50) %></br>
              <%= link_to article.user.name,user_path(article.user.id) %>
              </td>
              <td><%= article.created_at.strftime('%Y/%m/%d %H:%M') %></td>
              <td><%= render 'public/favorites/btn', article: article %></td>
              <td>コメント数: <%= article.article_comments.count %></td>
            </tr>
          </table>
        </div>
  <div class="card-body col" style="word-wrap: break-word;">
    <blockquote class="blockquote mb-0">
      <p><%= link_to article.title, article_path(article.id) %></p>
    </blockquote>
  </div>
  <% end %>
</div>
</div>
</div>
</div>

