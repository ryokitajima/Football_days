<h1>投稿一覧</h1>
<div>
  <% @articles.each do |article| %>
  <table>
    <tr>
      <td><%= article.user.name %></td>
      <td><%= article.created_at.strftime('%Y/%m/%d') %></td>
    </tr>
    <tr>
      <td><%= article.title %></td>
    </tr>
  </table>
  <% end %>
</div>

