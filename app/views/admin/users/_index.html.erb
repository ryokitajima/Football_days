<table class='table'style="table-layout: fixed;">
  <thead>
    <tr>
      <th><span style="color: gray;">user</span></th>
      <th><span style="color: gray;">フォロー数</span></th>
      <th><span style="color: gray;">フォロワー数</span></th>
    </tr>
  </thead>
  <tbody>
    <% users.each do |user| %>
      <tr>
        <td style="word-wrap: break-word;">
          <% if user.image.attached? %>
            <%= link_to admin_user_path(user.id) do %>
            <%= image_tag user.get_image(50, 50), class: "bd-placeholder-img mr-3 rounded-circle", width: "50", height: "50"  %>
            <% end %>
          <% else %>
            <%= link_to admin_user_path(user.id) do %>
            <%= image_tag 'no_image.jpg', size: '50x50', class: "bd-placeholder-img mr-3 rounded-circle" %>
            <% end %>
          <% end %>
          <%= link_to user.name, admin_user_path(user.id), style: "color: black;" %>
        </td>
        <td><%= link_to user.followings.count, admin_user_followings_path(user.id), style: "color: black;" %></td>
        <td><%= link_to user.followers.count, admin_user_followers_path(user.id), style: "color: black;" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

